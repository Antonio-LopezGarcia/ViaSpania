import { describe,expect,it } from 'vitest';
import { DEFAULT_APP_SETTINGS,loadAppSettings,normalizePointName,recommendedCellLimit,saveAppSettings } from './appSettings';
describe('nombres de puntos',()=>{it('limita los nombres a veinte caracteres Unicode',()=>{expect(Array.from(normalizePointName('abcdefghijklmnopqrstuvw')).length).toBe(20);expect(normalizePointName('   Mi punto')).toBe('Mi punto')})});
describe('límite de procesado',()=>{it('recomienda límites conservadores según memoria',()=>{expect(recommendedCellLimit(8*1073741824)).toBe(2_000_000);expect(recommendedCellLimit(16*1073741824)).toBe(3_000_000);expect(recommendedCellLimit(24*1073741824)).toBe(3_500_000);expect(recommendedCellLimit(32*1073741824)).toBe(5_000_000)})});
describe('preferencias de interfaz',()=>{it('conserva los nuevos ajustes al guardar',()=>{const storage=new Map<string,string>();Object.defineProperty(globalThis,'localStorage',{value:{getItem:(key:string)=>storage.get(key)??null,setItem:(key:string,value:string)=>storage.set(key,value)},configurable:true});const settings={...DEFAULT_APP_SETTINGS,showCrosshairs:false,showSelectionMarker:false,showScales:false,showSelectionCoordinates:false,showMdtCursorInfo:false,labelTextSizePx:15 as const,label3dOffsetM:35,label3dLeaderLine:false,terrain3dDetached:true,startupProject:'last' as const};saveAppSettings(settings);expect(loadAppSettings()).toMatchObject(settings)})});

it('migra preferencias antiguas y conserva la activación de superficies',()=>{localStorage.setItem('viaspania.settings.v1',JSON.stringify({showCrosshairs:false,enabledSurfaceModels:{mds05:true,copernicus30:false}}));expect(loadAppSettings().enabledElevationModels).toEqual({mdt5:true,mdt25:true,mdt200:true,mds05:true,copernicus30:false})});
