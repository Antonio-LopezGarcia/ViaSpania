function palette(){const result:number[]=[];for(let index=0;index<256;index++)result.push(((index>>5)&7)*255/7,((index>>2)&7)*255/7,(index&3)*255/3);return result.map(Math.round)}
function indexed(pixels:Uint8ClampedArray){const output=new Uint8Array(pixels.length/4);for(let i=0;i<output.length;i++){const o=i*4;output[i]=(pixels[o]>>5)<<5|(pixels[o+1]>>5)<<2|(pixels[o+2]>>6)}return output}
function lzw(data:Uint8Array){const clear=256,end=257,codes:number[]=[];for(let index=0;index<data.length;index+=250)codes.push(clear,...data.subarray(index,index+250));codes.push(end);let buffer=0,bits=0;const packed:number[]=[];for(const code of codes){buffer|=code<<bits;bits+=9;while(bits>=8){packed.push(buffer&255);buffer>>=8;bits-=8}}if(bits)packed.push(buffer&255);const bytes:number[]=[8];for(let index=0;index<packed.length;index+=255)bytes.push(Math.min(255,packed.length-index),...packed.slice(index,index+255));bytes.push(0);return bytes}
const word=(value:number)=>[value&255,value>>8&255];
function append(target:number[],values:readonly number[]){for(let index=0;index<values.length;index+=8192)target.push(...values.slice(index,index+8192))}
export function encodeGif(frames:ImageData[],delayMs:number){if(!frames.length)throw new Error('No hay fotogramas para crear el GIF.');const{width,height}=frames[0],delay=Math.min(65535,Math.max(2,Math.round(delayMs/10))),out:number[]=[71,73,70,56,57,97,...word(width),...word(height),247,0,0,...palette(),33,255,11,...Array.from('NETSCAPE2.0').map(c=>c.charCodeAt(0)),3,1,0,0,0];for(const frame of frames){out.push(33,249,4,0,...word(delay),0,0,44,0,0,0,0,...word(width),...word(height),0);append(out,lzw(indexed(frame.data)))}out.push(59);return new Uint8Array(out)}
